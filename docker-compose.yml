services:
  aac-app:
    build: .
    container_name: aac-app
    ports:
      - "8085:8085"
    environment:
      - AAC_DATA_DIR=/app/data
    volumes:
      - /opt/aac-data:/app/data
    restart: unless-stopped
